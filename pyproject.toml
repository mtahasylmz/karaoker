[tool.uv.workspace]
members = [
  "packages/shared-py",
  "stages/separate",
  "stages/transcribe",
]
# Future: "stages/align", "stages/record-mix".

[tool.uv.sources]
annemusic-shared = { workspace = true }

[tool.uv]
# Use a single lockfile across the workspace.
