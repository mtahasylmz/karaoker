[tool.uv.workspace]
members = [
  "packages/shared-py",
  "stages/separate",
]
# Members are added as we port each stage in Phase D.
# Future: "stages/transcribe", "stages/align", "stages/record-mix".

[tool.uv.sources]
annemusic-shared = { workspace = true }

[tool.uv]
# Use a single lockfile across the workspace.
